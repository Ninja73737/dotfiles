#!/usr/bin/env bash

# Programs

alt + Return
    alacritty

alt + shift + Return
    alacritty -e fish -C 'lf'

alt + space
    rofi -show drun

alt + slash
    thunar

alt + shift + c
    rofi -show calc -modi calc -no-show-match -no-sort

alt + Tab
    rofi -show window

alt + shift + s
    pkill -USR1 -x sxhkd

alt + shift + o
    $HOME/.scripts/switch_output_linux

alt + shift + {Down,Up}
    pulsemixer --change-volume {-,+}1

alt + shift + {Left,Right,space}
    playerctl --player=spotify {previous,next,play-pause}

{XF86AudioLowerVolume,XF86AudioRaiseVolume}
    pulsemixer --change-volume {-,+}1

alt + shift + f
    flameshot gui

alt + shift + w
    $HOME/.scripts/random_feh_bg -u

# alt + shift + y
#     $HOME/.scripts/yt_mod_tools

# bspwm

alt + shift + b
    bspc node @/ --balance

alt + shift + {_,super + }{h,j,k,l}
    bash -c "COMMAND="{f,s}" && DIR="{west,south,north,east}" && bspc query -N -n \$DIR && bspc node -\$COMMAND \$DIR || bspc monitor -\$COMMAND \$DIR"

alt + shift + {_,super + }{1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}' --follow

alt + shift + {p,u,g,n}
    $HOME/.scripts/qutebrowser-profile --new {personal,university,gaming,mod}

alt + shift + {q,r}
    bspc {quit,wm -r}

alt + {w,q}
    bspc node -{c,k}

alt + backslash
    bspc node focused.tiled -t pseudo_tiled || bspc node focused.pseudo_tiled -t floating || bspc node focused.floating -t fullscreen || bspc node focused.fullscreen -t tiled

alt + shift + backslash
    bspc node focused.tiled -t fullscreen || bspc node focused.fullscreen -t floating || bspc node focused.floating -t pseudo_tiled || bspc node focused.pseudo_tiled -t tiled

# button4
#     xdotool key Caps_Lock; xdotool click 4; xdotool click 4; xdotool click 4; xdotool key Caps_Lock

# button5
#     xdotool key Caps_Lock; xdotool click 5; xdotool click 5; xdotool click 5; xdotool key Caps_Lock

alt + shift + i
    $HOME/.scripts/linein_listen_linux
